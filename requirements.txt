# Core (BlueDB data access)
pandas>=2.0
numpy>=1.24
# Geometry resolution and plotting (BlueGeo, examples)
geopandas>=0.14
shapely>=2.0
requests>=2.28
matplotlib>=3.7
# eval_ned.py writes an .xlsx discrepancy workbook
openpyxl>=3.1
